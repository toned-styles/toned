/** Compatibility for pre-consolidation condition builders. New code uses system.q. */
export type { Condition, ContainerConditionBuilder } from './conditions.ts'
export { and, bp, cq, not, or } from './conditions.ts'
